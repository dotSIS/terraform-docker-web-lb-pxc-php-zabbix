server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /opt/apps;

    index index.php index.html index.htm index.nginx-debian.html;

    server_name _;

    location / {
        proxy_pass http://pxc_node1;
        try_files $uri $uri/ =404;
    }
}
